// Desenvolvimento local: deixe apiBase vazio para detectar automaticamente localhost/LAN.
// No deploy do Render, render-build.sh gera este arquivo com a URL pública do backend.
window.CATALOGO_CONFIG = window.CATALOGO_CONFIG || { apiBase: '' };
